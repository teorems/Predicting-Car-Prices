## Predicting car prices

The project's objective is to use the numeric variables of a data set to set up two simple machine learning models, compare them, choose the best one given the performances on the train set and finally make predictions.